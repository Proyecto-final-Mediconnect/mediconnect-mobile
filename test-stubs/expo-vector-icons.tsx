/*
 * Stub de `@expo/vector-icons` para Vitest.
 *
 * El paquete real carga la fuente de íconos con `expo-font`, que es un módulo
 * nativo. Un ícono no tiene nada que afirmar en un test —los que importan llevan
 * `accessibilityLabel` en su contenedor—, así que no se dibuja.
 */
export function Ionicons(): null {
  return null;
}
